# ── Core: LangGraph + LangChain ─────────────────────────────
langgraph
langchain
langchain-core
langchain-community
langgraph-checkpoint-sqlite

# ── LLM providers ───────────────────────────────────────────
# Groq (free-tier hosted models)
langchain-groq
# Ollama (local models)
langchain-ollama
# ollama            # optional: the Python client (langchain-ollama pulls its own deps)
# langchain-anthropic
# langchain-openai

# ── RAG (add later as a tool) ───────────────────────────────
# Local embeddings — sentence-transformers pulls in torch (~110 MB wheel,
# ~800 MB on disk). Drop both if you switch to a hosted embedding API.
sentence-transformers
langchain-huggingface

# Vector stores
# FAISS is the one currently in use; chroma is kept as an alternative.
faiss-cpu
langchain-chroma
chromadb

# Document loading & splitting
pypdf
unstructured
tiktoken

# ── Config / utilities ──────────────────────────────────────
python-dotenv
pydantic

# ── UI ──────────────────────────────────────────────────────
# Required: app.py is a Streamlit app.
streamlit

# ── Optional: expose the chatbot over an API ────────────────
# fastapi
# uvicorn

# ── Optional: observability / debugging ─────────────────────
# langsmith
